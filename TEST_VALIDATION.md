# ✅ Валидация продуктов на бекенде

## 📋 Реализованные правила валидации

### 1. Имя продукта (name)
- ✅ **Обязательное поле**
- ✅ **Минимум:** 3 символа
- ✅ **Максимум:** 100 символов
- ✅ **Тип:** строка

### 2. Описание (description)
- ✅ **Опциональное поле**
- ✅ **Минимум:** 10 символов (если заполнено)
- ✅ **Максимум:** 500 символов
- ✅ **Тип:** строка
- 🎯 **Валидация только если поле не пустое** (`ValidateIf`)

### 3. Цена (price)
- ✅ **Обязательное поле**
- ✅ **Минимум:** $0.01
- ✅ **Максимум:** $999,999.99
- ✅ **Тип:** число

---

## 🧪 Примеры тестовых запросов

### ❌ Ошибка: Короткое имя

```bash
POST /products
{
  "name": "AB",  // Меньше 3 символов
  "price": 100
}
```

**Ответ:**
```json
{
  "statusCode": 400,
  "message": [
    "Имя продукта должно содержать минимум 3 символа"
  ],
  "error": "Bad Request"
}
```

---

### ❌ Ошибка: Длинное имя

```bash
POST /products
{
  "name": "Это очень длинное название продукта которое превышает максимальную длину в сто символов и должно вызвать ошибку валидации",
  "price": 100
}
```

**Ответ:**
```json
{
  "statusCode": 400,
  "message": [
    "Имя продукта не должно превышать 100 символов"
  ],
  "error": "Bad Request"
}
```

---

### ❌ Ошибка: Короткое описание

```bash
POST /products
{
  "name": "Хороший продукт",
  "description": "Коротко",  // Меньше 10 символов
  "price": 100
}
```

**Ответ:**
```json
{
  "statusCode": 400,
  "message": [
    "Описание должно содержать минимум 10 символов"
  ],
  "error": "Bad Request"
}
```

---

### ❌ Ошибка: Цена слишком мала

```bash
POST /products
{
  "name": "Продукт",
  "price": 0  // Меньше 0.01
}
```

**Ответ:**
```json
{
  "statusCode": 400,
  "message": [
    "Минимальная цена $0.01"
  ],
  "error": "Bad Request"
}
```

---

### ❌ Ошибка: Цена слишком велика

```bash
POST /products
{
  "name": "Продукт",
  "price": 1000000  // Больше 999999.99
}
```

**Ответ:**
```json
{
  "statusCode": 400,
  "message": [
    "Максимальная цена $999,999.99"
  ],
  "error": "Bad Request"
}
```

---

### ✅ Успешное создание (без описания)

```bash
POST /products
{
  "name": "Отличный продукт",
  "price": 99.99
}
```

**Ответ:** `201 Created`

---

### ✅ Успешное создание (с описанием)

```bash
POST /products
{
  "name": "Супер продукт",
  "description": "Это полное описание продукта с более чем десятью символами",
  "price": 299.99
}
```

**Ответ:** `201 Created`

---

## 🔧 Технические детали

### Используемые декораторы:

```typescript
@MinLength(3)      // Минимальная длина
@MaxLength(100)    // Максимальная длина
@Min(0.01)         // Минимальное значение
@Max(999999.99)    // Максимальное значение
@IsOptional()      // Опциональное поле
@ValidateIf()      // Условная валидация
```

### Глобальная конфигурация ValidationPipe:

```typescript
app.useGlobalPipes(new ValidationPipe({
  whitelist: true,           // Удаляет лишние свойства
  forbidNonWhitelisted: true, // Выбрасывает ошибку при лишних полях
  transform: true,            // Автопреобразование типов
}));
```

---

## 🎯 Преимущества

✅ **Защита от некорректных данных**  
✅ **Читаемые сообщения об ошибках на русском**  
✅ **Автоматическая валидация через декораторы**  
✅ **Согласованность с фронтендом**  
✅ **Описание остается опциональным, но если заполнено - валидируется**
